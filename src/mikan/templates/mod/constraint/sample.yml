constraint:
  type: <point|orient|parent|aim|scale>

  target: <target>::<node>
  node: <id>::<node>

  # optional
  weights: [1]
  maintain_offset: on

  # aim options
  aim: [0, 1, 0]
  up: [1, 0, 0]
